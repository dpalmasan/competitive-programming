#pragma once

#include <vector>
#include <utility>

bool has_unique_elements(std::vector<int>&);
int maximum_customers(std::vector<std::pair<int, int>>&);
std::vector<std::pair<int, int>> schedule_events(std::vector<std::pair<int, int>>&);